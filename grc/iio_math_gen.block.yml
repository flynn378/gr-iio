# auto-generated by grc.converter

id: math_gen
label: Function Generator
category: '[Industrial IO]/Waveform Generators'

parameters:
-   id: samp_rate
    label: samp_rate
    dtype: int
    default: samp_rate
-   id: wav_freq
    label: wav_freq
    dtype: int
    default: '1'
-   id: function
    label: f(x) =
    dtype: string

outputs:
-   domain: stream
    dtype: float
asserts:
- ${ function }

templates:
    imports: from gnuradio import iio
    make: iio.iio_math_gen(${samp_rate}, ${wav_freq}, ${function})

file_format: 1
