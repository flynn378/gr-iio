# auto-generated by grc.converter

id: iio_attr_source
label: IIO Attribute Source
category: '[Industrial IO]'

parameters:
-   id: uri
    label: uri
    dtype: string
    default: '""'
-   id: device
    label: Device
    dtype: string
    default: '""'
-   id: attr_type
    label: Attribute Type
    dtype: enum
    options: ['0', '1', '2', '3']
    option_labels: [Channel, Device, Device Debug, Register]
    hide: part
-   id: output
    label: Input/Output
    dtype: bool
    options: ['False', 'True']
    option_labels: [Input, Output]
    hide: ${ ('none' if int(attr_type) == 0 else 'all') }
-   id: channel
    label: Channel Name
    dtype: string
    default: '""'
    hide: ${ ('none' if int(attr_type) == 0 else 'all') }
-   id: attribute
    label: Attribute Name
    dtype: string
    default: '""'
    hide: ${ ('none' if int(attr_type) != 3 else 'all') }
-   id: address
    label: Register Address
    dtype: int
    default: int("0x123",0)
    hide: ${ ('none' if int(attr_type) == 3 else 'all') }
-   id: required_enable
    label: Required Enable
    dtype: bool
    options: ['False', 'True']
    option_labels: [Not Required, Enable]
    hide: ${ ('none' if int(attr_type) == 3 else 'all') }
-   id: type
    label: Data Type
    dtype: enum
    options: ['0', '1', '2', '3', '4']
    option_labels: [Double, Float (casted from double), Long Long, Long (casted from
            long long), Bool]
    option_attributes:
        fcn: [f64, f32, s64, s32, s8]
    hide: ${ ('none' if int(attr_type) != 3 else 'all') }
-   id: update_interval_ms
    label: Update Interval (ms)
    dtype: int
    default: '1'
-   id: samples_per_update
    label: Samples Per Update
    dtype: int
    default: '1024'

outputs:
-   domain: stream
    dtype: ${ ('s32' if int(attr_type) == 3 else str(type.fcn)) }
    multiplicity: ${ len(attributes) }

templates:
    imports: from gnuradio import iio
    make: iio.attr_source(${uri}, ${device}, ${channel}, ${attribute}, ${update_interval_ms},
        ${samples_per_update}, ${type}, ${attr_type}, ${output}, ${address}, ${required_enable})

documentation: |-
    https://wiki.analog.com/resources/tools-software/linux-software/gnuradio

file_format: 1
