# auto-generated by grc.converter

id: pluto_source
label: PlutoSDR Source
category: '[Industrial IO]/PlutoSDR'
flags: throttle

parameters:
-   id: uri
    label: Device URI
    dtype: string
-   id: frequency
    label: LO Frequency
    dtype: real
    default: '2400000000'
-   id: samplerate
    label: Sample rate
    dtype: real
    default: '2084000'
-   id: bandwidth
    label: RF bandwidth
    dtype: real
    default: '20000000'
-   id: buffer_size
    label: Buffer size
    dtype: int
    default: '0x8000'
-   id: quadrature
    label: Quadrature
    dtype: bool
    default: 'True'
    options: ['True', 'False']
-   id: rfdc
    label: RF DC
    dtype: bool
    default: 'True'
    options: ['True', 'False']
-   id: bbdc
    label: BB DC
    dtype: bool
    default: 'True'
    options: ['True', 'False']
-   id: gain
    label: Gain Mode
    dtype: enum
    options: ['"manual"', '"slow_attack"', '"fast_attack"', '"hybrid"']
    option_labels: [Manual, Slow, Fast, Hybrid]
-   id: manual_gain
    label: Manual Gain (dB)
    dtype: real
    default: '64.0'
    hide: ${ ('none' if gain == '"manual"' else 'all') }
-   id: filter
    label: Filter
    dtype: file_open
-   id: auto_filter
    label: Filter auto
    dtype: bool
    default: 'True'
    options: ['True', 'False']

outputs:
-   domain: stream
    dtype: complex
    multiplicity: '1'
asserts:
- ${ (samplerate >= 2084000) or (len(filter) > 0) or auto_filter }
- ${ (frequency >= 0) }
- ${ (bandwidth >= 0) }
- ${ (samplerate >= 0) }
- ${ not (auto_filter and len(filter)) }

templates:
    imports: from gnuradio import iio
    make: iio.pluto_source(${uri}, int(${frequency}), int(${samplerate}), int(${bandwidth}),
        ${buffer_size}, ${quadrature}, ${rfdc}, ${bbdc}, ${gain}, ${manual_gain},
        ${filter}, ${auto_filter})
    callbacks:
    - set_params(int(${frequency}), int(${samplerate}), int(${bandwidth}), ${quadrature},
        ${rfdc}, ${bbdc}, ${gain}, ${manual_gain}, ${filter}, ${auto_filter})

file_format: 1
