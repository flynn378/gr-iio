# auto-generated by grc.converter

id: math
label: Function
category: '[Industrial IO]/Math Operators'

parameters:
-   id: function
    label: f(x) =
    dtype: string
-   id: num_inputs
    label: Num Inputs
    dtype: int
    default: '1'
    hide: part

inputs:
-   domain: stream
    dtype: float
    multiplicity: ${ num_inputs }

outputs:
-   domain: stream
    dtype: float
asserts:
- ${ function }
- ${ num_inputs > 0 }

templates:
    imports: from gnuradio import iio
    make: iio.iio_math(${function}, ${num_inputs})

file_format: 1
