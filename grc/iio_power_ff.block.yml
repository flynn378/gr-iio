# auto-generated by grc.converter

id: iio_power_ff
label: Power
category: '[Industrial IO]/Math Operators'

parameters:
-   id: vlen
    label: Vec Length
    dtype: int
    default: '1'
    hide: ${ 'part' if vlen == 1 else 'none' }

inputs:
-   label: base
    domain: stream
    dtype: float
    vlen: ${ vlen }
-   label: exp
    domain: stream
    dtype: float
    vlen: ${ vlen }

outputs:
-   domain: stream
    dtype: float
    vlen: ${ vlen }
asserts:
- ${ vlen > 0 }

templates:
    imports: from gnuradio import iio
    make: iio.power_ff(${vlen})

file_format: 1
