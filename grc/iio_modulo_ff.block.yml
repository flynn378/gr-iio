# auto-generated by grc.converter

id: iio_modulo_ff
label: Modulo
category: '[Industrial IO]/Math Operators'

parameters:
-   id: vlen
    label: Vec Length
    dtype: int
    default: '1'
    hide: ${ 'part' if vlen == 1 else 'none' }

inputs:
-   domain: stream
    dtype: float
    vlen: ${ vlen }
-   label: mod
    domain: stream
    dtype: float
    vlen: ${ vlen }

outputs:
-   domain: stream
    dtype: float
    vlen: ${ vlen }
asserts:
- ${ vlen > 0 }

templates:
    imports: from gnuradio import iio
    make: iio.modulo_ff(${vlen})

file_format: 1
