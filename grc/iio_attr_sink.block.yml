# auto-generated by grc.converter

id: iio_attr_sink
label: IIO Attribute Sink
category: '[Industrial IO]'

parameters:
-   id: uri
    label: uri
    dtype: string
    default: '""'
-   id: device
    label: Device
    dtype: string
    default: '""'
-   id: attr_type
    label: Attribute Type
    dtype: enum
    options: ['0', '1', '2', '3', '4']
    option_labels: [Channel, Device, Device Buffer, Device Debug, Register]
-   id: required_enable
    label: Required Enable
    dtype: bool
    options: ['False', 'True']
    option_labels: [Not Required, Enable]
    hide: ${ ('none' if int(attr_type) == 4 else 'all') }
-   id: output
    label: Input/Output
    dtype: bool
    options: ['False', 'True']
    option_labels: [Input, Output]
    hide: ${ ('none' if int(attr_type) == 0 else 'all') }
-   id: channel
    label: Channel Name
    dtype: string
    default: '""'
    hide: ${ ('none' if int(attr_type) == 0 else 'all') }

inputs:
-   domain: message
    id: attr
    optional: true

templates:
    imports: from gnuradio import iio
    make: iio.attr_sink(${uri}, ${device}, ${channel}, ${attr_type}, ${output}, ${required_enable})

documentation: |-
    https://wiki.analog.com/resources/tools-software/linux-software/gnuradio

file_format: 1
